# Segredos do deploy (fora do Git)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxx
# Token de leitura do dataset PRIVADO (preferir um read-only p/ o container).
# Vazio = usa o proprio HF_TOKEN.
HF_TOKEN_READ=
# Deixe vazio para herdar do .env da raiz do projeto
DEEPSEEK_API_KEY=
